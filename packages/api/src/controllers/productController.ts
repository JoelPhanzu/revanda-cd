// Contrôleur des produits
import { Request, Response } from 'express';

export const getAllProducts = async (_req: Request, res: Response) => {
  // À implémenter
  res.json({ message: 'Produits' });
};